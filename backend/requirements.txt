fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.2
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Batch 11.1 -- pinned below 4.0: passlib 1.7.4 (last released 2020)
# reads bcrypt's `__about__.__version__` attribute to detect the backend
# version, which bcrypt>=4.0 removed. Depending on the exact bcrypt/
# passlib patch combination this either logs a "(trapped) error reading
# bcrypt version" warning on every hash/verify call, or can fail
# password hashing outright -- a well-documented ecosystem
# incompatibility (see pyca/bcrypt#684 and related passlib issues), not
# something specific to this app. 3.2.2 is the last stable 3.x release
# and is fully compatible with passlib 1.7.4 with no code changes needed
# on our side.
bcrypt==3.2.2
python-multipart==0.0.9
email-validator==2.2.0
pandas==2.2.3
openpyxl==3.1.5
# Google Sheets integration (Step 9) -- OAuth token exchange/refresh
# calls and Fernet encryption of stored tokens. cryptography is also a
# transitive dep of python-jose[cryptography] above, but pinned
# explicitly here since app/services/google_oauth.py imports it directly.
httpx==0.28.1
cryptography==46.0.6
stripe==15.6.0
slowapi==0.1.10
apscheduler==3.11.3
# Error monitoring (Step 10, Batch 10.8) -- inert until SENTRY_DSN is
# set (see app/core/monitoring.py), so this has no effect on local/dev/
# test environments without a Sentry account.
sentry-sdk[fastapi,sqlalchemy]==2.19.2
# Range rather than an exact pin: verify/lock the current release for your
# environment (`pip install openai` then freeze) rather than trusting this
# one, since it wasn't resolved against a live package index here. Groq's
# API is OpenAI-compatible, so this is the same SDK, just pointed at
# Groq's base_url in app/services/ai_assistant.py.
openai>=1.40.0,<2.0.0
