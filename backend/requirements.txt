fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.2
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
email-validator==2.2.0
pandas==2.2.3
openpyxl==3.1.5
# Range rather than an exact pin: verify/lock the current release for your
# environment (`pip install anthropic` then freeze) rather than trusting
# this one, since it wasn't resolved against a live package index here.
anthropic>=0.40.0,<1.0.0
